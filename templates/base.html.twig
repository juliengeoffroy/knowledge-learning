<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Knowledge Learning{% endblock %}</title>
    <link rel="icon" href="{{ asset('images/Favicon.png') }}">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navigation -->
    <header class="navbar">
        <div class="nav-logo">
            <img src="{{ asset('images/Logo.jpg') }}" alt="Knowledge Logo">
        </div>
        <nav class="nav-links">
            <a href="{{ path('app_home') }}">Accueil</a>
            <a href="{{ path('app_courses') }}">Cours</a>
            <a href="{{ path('app_my_courses') }}">Mes cours</a>
            <a href="{{ path('app_certifications') }}">Mes Certifications</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin') }}">Administration</a>
            {% endif %}
            {% if app.user %}
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
        </nav>
    </header>

    <!-- Contenu principal -->
    <main>
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer">
        <p>© 2025 KNOWLEDGE LEARNING — Tous droits réservés</p>
        <div class="social-links">
            <a href="#">Facebook</a> |
            <a href="#">Twitter</a> |
            <a href="#">LinkedIn</a>
        </div>
    </footer>
</body>
</html>
